@model ToDoList.Models.TasksListViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1 class="text-center mb-4">Edit Tasks List</h1>

<div class="row justify-content-center">
    <div class="col-md-8">
        <form asp-action="Edit" id="form" class="shadow-sm p-4 bg-light rounded">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group mb-4">
                <label asp-for="Name" class="control-label">List Name</label>
                <input asp-for="Name" class="form-control" placeholder="Enter task list name" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group mb-4">
                <label asp-for="Date" class="control-label">Select Date</label>
                <input asp-for="Date" type="date" class="form-control" />
                <span asp-validation-for="Date" class="text-danger"></span>
            </div>
            <h4 class="mt-4">Tasks</h4>
            <div id="tasks-container" class="mb-4">
                @Html.EditorFor(m => m.Tasks)
            </div>
            <button type="button" id="add-task" onclick="addTask()" class="btn btn-outline-primary mb-4">
                <i class="bi bi-plus-circle"></i> Add Task
            </button>
            <div class="form-group mb-3">
                <input type="submit" value="Save Changes" class="btn btn-success w-100" />
            </div>
        </form>
    </div>
</div>

<div class="mt-3 text-center">
    <a asp-action="Index" class="btn btn-link">Back to List</a>
</div>

@section Scripts {
    <script src="~/js/task.js"></script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
